## 01. Marketplace
Reads a product name ("Banana", "Apple", or "Kiwi") and a day type ("Weekday" or "Weekend") from the console.  
Checks the price based on the selected product and day, then prints the result to the console.

## 02. Largest Number Out of Three  
Reads three integer numbers from the console.  
Compares the numbers and prints the largest one among them.

## 03. Bonus Score  
Reads an integer number representing points from the console.  
Calculates and adds bonus points based on specific ranges (0-3, 4-6, or 7-9) and prints the final score.

## 04. Food or Drink  
Reads a product name as a string from the console.  
Categorizes the input into `"food"`, `"drink"`, or `"unknown"` based on a predefined list of items and prints the category.

## 05. Invalid Number  
Reads an integer from the console.  
A number is considered valid if it is within the range [100...200] or equals 0. If the entered number does not meet these criteria, the program prints `"invalid"`.
